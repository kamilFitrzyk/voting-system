create table if not exists USERS(
                      ID int not null,
                      NAME varchar(100) not null,
                      STATUS int,
                      PRIMARY KEY ( ID )
);